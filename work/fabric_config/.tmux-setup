#!/usr/bin/env bash

export TMUX_SESSION
TMUX_SESSION=$(tmux display-message -p "#S")

windows=('lazygit' 'build' 'xfer' 'ssh-tunnel')

for window in "${windows[@]}"; do
  tmux-windowizer "$window"
done
